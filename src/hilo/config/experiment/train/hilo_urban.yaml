# @package _global_

defaults:
  - override /model: hilo
  - override /data: urban_fusion_filtered
  - override /training/loss/giou_loss: non_rotated_bev_giou

name: Urban_HiLO

logging:
  checkpoints:
    enabled: True
    metrics: [mAP, mean_F1]

model:
  detection_normalization:
    enabled: True
  detection_embedding:
    enabled: True